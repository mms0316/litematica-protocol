main: xyz.holocons.mc.litematicaprotocol.paper.LitematicaProtocolPlugin
name: ${name}
version: ${version}
description: ${description}
api-version: ${apiVersion}
authors: [dlee13]
website: holocons.xyz
depend: [WorldEdit]
prefix: LitematicaProtocol
permissions:
  worldedit.clipboard.protocol:
    default: op
